#include "main.h"


/**
 * _abs - computing the absolute value of an integer
 * @n: the specific integer to check
 * Return: Absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

